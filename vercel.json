{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "buildCommand": "rm -rf vault && git clone --depth 1 https://github.com/spongeclub/spongeclub_1.git vault && VAULT_PATH=./vault npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install"
}
